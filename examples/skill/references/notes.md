# Reference material

Files in `references/` are copied verbatim alongside the skill for every agent. Use
them for material the agent should read on demand rather than carry in context: API
schemas, query snippets, long checklists, worked examples.

Keep `SKILL.md` short and point at these from it.
